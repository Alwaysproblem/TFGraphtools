modelURL:
  needfix: 
    - name: "bert24-ipu"
    - tag: "serve"
  standard:
    - name: "bert24orig"
    - tag: "serve"

outputNode:
  - 'similarity/mlp/output_layer/add:0'

CheckOutputConfig:
  - threshold: 1e-3